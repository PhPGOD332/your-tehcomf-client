export interface TFormInputs {
    firstName: string;
    mobilePhone: string;
    note: string;
    date: string;
}
export enum TFormInputsNames {
    firstName = 'firstName',
    mobilePhone = 'mobilePhone',
    note = 'note',
    date = 'date',
}